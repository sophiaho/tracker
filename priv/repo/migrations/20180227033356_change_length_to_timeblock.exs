defmodule Tracker.Repo.Migrations.ChangeLengthToTimeblock do
  use Ecto.Migration

  def change do
    #alter table(:tasks) do
    #  add :time, references(:time_blocks)

    #  timestamps()
  #  end

  end
end
